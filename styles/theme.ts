import {
    color,
    space,
    radius,
    shadow,
    typography
} from './tokens'

export const theme = {
    color,
    space,
    radius,
    shadow,
    typography
}

export type AppTheme = typeof theme